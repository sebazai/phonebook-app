
<ul>
  <% @contacts.each do |contact| %>
    <li><%= link_to contact.full_name, contact_path(contact) %> </li>
  <% end %>

  <p><%= button_to "Add a new contact", new_contact_path, method: :get %></p>
</ul>