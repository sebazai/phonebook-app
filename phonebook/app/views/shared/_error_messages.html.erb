<% if object.errors.any? %>
<div>
  <h3><%= pluralize(object.errors.count, "error") %> prohibited this contact from being saved</h3>

  <ul>
    <% object.errors.each do |error| %>
      <li><%= error.full_message %></li>
    <% end %>
  </ul>
</div>
<% end %>